platform :ios, '10.0'
use_frameworks!

target 'weathercast-demo' do
  pod 'WeakArray', git: 'https://github.com/n-studio/WeakArray.git', branch: 'custom'
end

target 'weathercast-demoTests' do
  pod 'Hippolyte'
end

